# Add header files
set(HEADER_FILES
    Logger.h
    Multiverse.h
    Universe.h
    # ... add other header files
)

# Include directories
target_include_directories(umgebung PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
